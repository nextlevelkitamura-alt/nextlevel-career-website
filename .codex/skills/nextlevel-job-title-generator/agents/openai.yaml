interface:
  display_name: "NextLevel Job Title Generator"
  short_description: "既存掲載トーンに合う求人タイトル案を生成"
  default_prompt: "Use $nextlevel-job-title-generator to create varied job title candidates that match the existing NextLevel listing tone."
